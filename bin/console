#!/usr/bin/env ruby
# frozen_string_literal: true

# Loads the Gemfile and configures +$LOAD_PATH+ so the gems can be required.
require 'bundler/setup'

# This gem.
require 'omniauth/bcrypt'

# The interactive Ruby console.
require 'irb'

# Starts IRB with the gem loaded.
IRB.start(__FILE__)
